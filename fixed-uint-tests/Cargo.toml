[package]
name = "numext-fixed-uint-tests"
version = "0.1.0"
authors = ["Boyu Yang <yangby@cryptape.com>"]

[dependencies]
numext-fixed-uint = { path = "../fixed-uint", features = ["impl_rand"] }
uint = { version = "0.4", features = ["std"] }
proptest = "0.8"
rand = "0.5"

[dev-dependencies]
criterion = "0.2"

[[bench]]
name = "std_ops_arith"
harness = false

[[bench]]
name = "std_ops_shift"
harness = false
